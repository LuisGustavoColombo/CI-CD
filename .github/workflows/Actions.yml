name: Actions Workflow

on: [push]

jobs: 
  run-github-actions:
    runs-on: ubuntu-latest
    steps: 
      - name: Run image
        run: |
          apt install build-essential
      - name: Checkout
        uses: actions/checkout@v2